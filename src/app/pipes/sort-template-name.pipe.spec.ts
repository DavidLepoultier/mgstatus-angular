import { SortTemplateNamePipe } from './sort-template-name.pipe';

describe('SortTemplateNamePipe', () => {
  it('create an instance', () => {
    const pipe = new SortTemplateNamePipe();
    expect(pipe).toBeTruthy();
  });
});
