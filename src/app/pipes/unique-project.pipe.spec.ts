import { UniqueProjectPipe } from './unique-project.pipe';

describe('UniqueProjectPipe', () => {
  it('create an instance', () => {
    const pipe = new UniqueProjectPipe();
    expect(pipe).toBeTruthy();
  });
});
